<h1>All Stores</h1>

<%= link_to "Make A New Store!", new_store_path %>

<% @stores.each() do |item| %>
    <hr>
    <h3><%= item.name %></h3>
    <%= link_to "see more", store_path(item.id) %>

<% end %>